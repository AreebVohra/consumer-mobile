import { StyleSheet } from 'react-native';

const commonStyles = StyleSheet.create({
  subTextColor: {
    color: '#545454',
  },
  loginRegistrationInputFieldColor: {
    backgroundColor: '#eff0f2',
  },
  link: {
    color: '#411361',
  },
});

export default commonStyles;
